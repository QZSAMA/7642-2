Overleaf writeup link: https://www.overleaf.com/read/wngmycrdysty#2dbb9a

Code Location: 

Step on how to run the code:
    1. Unzip the A2.zip
    2. Run major experiments by below command (if mac/linux, you might need to use python3 instead of python):
        python optimization.py
    3. For all the other sub experiment graph, use Jupyter notebook to open the below file:
	E2 problem size.ipynb
	nn-final.ipynb
	

For any error you might encounter, that might be the Prerequisites missing, below is the Prerequisites:

Prerequisites
    Python3 (my version is 3.10.9)

    Library need to be install if missing/version not match , here is the command to install:
        pip install scikit-learn==1.2.1
        pip install numpy==1.23.5
        pip install matplotlib==3.7.0